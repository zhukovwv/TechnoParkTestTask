# ❓ Вопрос №1
### 1. Структура проекта 
```text
src/
├── api/           # FastAPI роутеры (HTTP слой)
├── services/      # Бизнес-логика
├── db/            # SQLAlchemy модели, сессии, репозитории
├── schemas/       # Pydantic-схемы (request/response)
├── core/          # Конфиг, логирование, middleware
└── main.py        # Точка входа
```
### 2. Бизнес-логика:

Бизнес-логика вынесена в слой `services`, так как она не должна зависеть от HTTP-слоя или способа хранения данных.
Такое разделение упрощает тестирование, повторное использование кода и соответствует принципу единой ответственности.
### 3. Конфигурация (env):

Конфигурация загружается через `pydantic-settings` с классом `Settings` в `core/settings.py`.
Переменные берутся из `.env` файла, проходят типизацию и валидацию на этапе запуска приложения.
### 4. Логирование:
Для логирования используется structlog.
Преимущества:
- структурированные логи в JSON-формате;
- удобная интеграция с ELK/Opensearch;
- поддержка контекстных данных (например, `request_id`);
- соответствует production best-practice для контейнеризированных сервисов.

### 5. Обработка ошибок:
- валидация входных данных — Pydantic → HTTP 422;
- ошибки базы данных — перехват исключений SQLAlchemy → HTTP 500;
- бизнес-ошибки — кастомные исключения в `services` → HTTP 400/409;
- глобальный middleware — единый формат ошибок и логирование.
# ❓ Вопрос №2
### 1. Почему здесь есть проблема со стоимостью?

Проблема связана с несогласованными единицами измерения. Один и тот же материал хранится в `kg` и `ton`, при этом цена указана без корректного пересчёта.

Фактически `0.1 ton = 100 kg`, но:
- `100 kg × 23.5 руб/кг = 2350 руб`
- `0.1 ton × 2.35 руб/тонна = 0.235 руб`

Основные причины:
- цена не привязана к базовой единице измерения;
- отсутствует валидация `qty`, `unit` и `unit_cost`;
- денежные значения хранятся в вещественном типе (`FLOAT`).

### 2. Как привести unit к единой системе?

Необходимо выбрать базовую единицу измерения (например, `kg`) и хранить все расчётные значения только в ней.

При сохранении данных:
- количество (`qty`) и цена (`unit_cost`) автоматически конвертируются в базовую единицу;
- коэффициенты конвертации хранятся в отдельном справочнике;
- исходные значения могут сохраняться для аудита.

### 3. Где должен находиться мэппинг единиц измерения?

Мэппинг единиц измерения должен храниться в базе данных в виде справочника.

Почему в БД:
- изменения не требуют деплоя кода;
- обеспечивается консистентность данных;
- возможна история изменений через миграции;
- решение масштабируется на несколько сервисов.

Кэширование (in-memory / Redis) допустимо в production-нагрузке,  
но для данного тестового задания является избыточным решением.

### 4. Как обеспечить консистентность данных?

Консистентность должна обеспечиваться на нескольких уровнях.

**Уровень приложения:**
- валидация входных данных с помощью Pydantic.

**Уровень базы данных:**
- CHECK-constraints (`qty > 0`, `unit_cost > 0`);
- FOREIGN KEY на справочник единиц измерения;
- уникальные индексы для предотвращения дубликатов;
- автоматическая нормализация данных (триггеры или логика приложения).

**Контроль качества:**
- юнит- и интеграционные тесты;
- мониторинг и алёрты на аномальные значения.

# Тестовое задание: Сервис расчёта стоимости

## 🚀 Быстрый запуск
```bash

# 1. Клонируйте репозиторий
git clone <repository-url>
cd <project-directory>

# 2. Запустите сервис
docker-compose up --build

# 3. Проверьте работу
curl http://localhost:8000/health
```
##  SQL-запрос, который возвращает 10 последних расчётов отсортированных по дате
```sql
SELECT id, total_cost_rub, created_at
FROM calc_results
ORDER BY created_at DESC
LIMIT 10;
```

